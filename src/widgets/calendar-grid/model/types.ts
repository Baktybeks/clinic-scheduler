export interface AppointmentPosition {
  top: number;
  height: number;
  zIndex: number;
}

export interface TimeSlotConfig {
  startHour: number;
  endHour: number;
  slotDuration: number;
  slotHeight: number;
}
